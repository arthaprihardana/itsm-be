import { StakeholderInputDto } from './stakeholder-input.dto';

describe('StakeholderInputDto', () => {
  it('should be defined', () => {
    expect(new StakeholderInputDto()).toBeDefined();
  });
});
