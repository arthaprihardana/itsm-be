import { DepartmentAssembler } from './department.assembler';

describe('DepartmentAssembler', () => {
  it('should be defined', () => {
    expect(new DepartmentAssembler()).toBeDefined();
  });
});
