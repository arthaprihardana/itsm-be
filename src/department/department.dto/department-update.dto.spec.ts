import { DepartmentUpdateDto } from './department-update.dto';

describe('DepartmentUpdateDto', () => {
  it('should be defined', () => {
    expect(new DepartmentUpdateDto()).toBeDefined();
  });
});
